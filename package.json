{
  "name": "ezzstar_presale",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ezzstar_presale",
    "create-local": "graph create --node http://localhost:8020/ ezzstar_presale",
    "remove-local": "graph remove --node http://localhost:8020/ ezzstar_presale",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ezzstar_presale",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.81.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
